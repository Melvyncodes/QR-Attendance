{
  "name": "campustrack-nfc-server",
  "version": "1.0.0",
  "description": "CampusTrack NFC server — uses Windows PC/SC via PowerShell, no native modules",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  }
}
